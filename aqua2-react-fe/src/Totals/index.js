import { default as ListTotals } from "./TotalsList";
import * as actions from "./totals_actions";
import reducer from "./totals_reducer";
import * as selectors from "./totals_selectors";

export default { ListTotals, actions, reducer, selectors };
